## Frontend env (Vite)
# Copy this file to `.env` at project root (same level as package.json)
# Base URL of your backend API (no trailing slash recommended)
VITE_API_BASE_URL=http://localhost:3000

# AWS Cognito (Amplify Auth)
# (defaults are kept in code, set these to override per environment)
VITE_COGNITO_USER_POOL_ID=us-east-1_ilPTEVT0U
VITE_COGNITO_USER_POOL_CLIENT_ID=1c50sakb0hn0k5grcgr4atbe8n


